{
  "name": "pressthebutton-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node dist/server.js",
    "test": "./node_modules/.bin/mocha src/**/*.spec.js --compilers js:babel-register --recursive",
    "test:watch": "npm run test -- --watch",
    "compile": "babel src --out-dir dist --ignore *.spec.js",
    "compile:watch": "babel src --watch --out-dir dist --ignore *.spec.js"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-core": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-register": "^6.9.0",
    "chai": "^3.5.0",
    "eslint": "^2.10.2",
    "eslint-config-airbnb-base": "^3.0.1",
    "eslint-plugin-import": "^1.8.0",
    "mocha": "^2.5.1"
  },
  "dependencies": {
    "node-uuid": "^1.4.7",
    "websocket": "^1.0.23"
  }
}
